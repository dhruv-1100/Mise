# Regenerated by the seeding step from the .dc.html artboards; the artboards
# and canvas.json are the source. Committing a ~2 MiB build artifact that
# changes wholesale on every edit would bury the actual diffs.
mise-design-system.html
